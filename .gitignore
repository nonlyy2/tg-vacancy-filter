# --- secrets ---
.env
.env.local
.env.*.local
session.json
*.session
session.b64
*.b64

# --- runtime / state ---
# state.json is deliberately NOT ignored: the scheduled workflow commits it
# back so the poll cursor survives between runs.
state.json.tmp
matches.jsonl
matches.*.jsonl

# --- build artifacts ---
/bin/
/bot
/tmp/
/dist/
*.log
*.test
*.out
coverage.*

# --- editor / OS ---
.idea/
.vscode/
.DS_Store
Thumbs.db
*.swp
*~

# --- Go module cache (only meaningful when vendoring) ---
/vendor/

CLAUDE.md
/private/
